import React from "react";
import DrumMachine from "./drumMachine"; // Make sure the path is correct
import "./drumMachine.css"; // Make sure the path is correct

function App() {
  return (
    <div className="App">
      <DrumMachine />
    </div>
  );
}

export default App;
